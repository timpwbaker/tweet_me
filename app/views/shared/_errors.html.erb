<% if record.errors.any? %>
  <div class="alert alert-danger">
    <p>
      This <%= record.class.name.downcase %> couldn't be sent because the 
      <%= record.errors.full_messages.map(&:downcase).to_sentence %>
    </p>
  </div>
<% end %>
